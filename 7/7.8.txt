read() defines item as a plain reference because we need to use the reference to modify the object it refers to. print() defines item as a const reference because we have no need to use the reference to modify the object it refers to.
