front_inserter() produces an iterator which repeatedly uses push_front() to insert elements.
back_inserter() produces an iterator which repeatedly uses push_back() to insert elements.
inserter() produces an iterator which keeps inserting elements before a specified iterator.
