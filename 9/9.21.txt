The loop would work exactly the same as it did with the list. it would be a lot slower because the insertion would involve moving all elements one position to the right before inserting the new element.
